# ─── CORE CLI TOOLS ───────────────────────────────────────────────────────────
brew "bat"                    # Better cat
brew "eza"                    # Better ls
brew "fd"                     # Better find
brew "fzf"                    # Fuzzy finder
brew "ripgrep"                # Better grep
brew "zoxide"                 # Better cd
brew "jq"                     # JSON processor
brew "tldr"                   # Simplified man pages
brew "wget"                   # HTTP client
brew "tree-sitter"            # Parser generator (shared by nvim + emacs)

# ─── GIT ──────────────────────────────────────────────────────────────────────
brew "git"
brew "git-delta"              # Better git diffs
brew "gh"                     # GitHub CLI
brew "lazygit"                # TUI for git

# ─── EDITORS ──────────────────────────────────────────────────────────────────
brew "neovim"
brew "gcc"                    # Required for Emacs native compilation
cask "emacs-plus-app"         # Emacs 30 with native comp

# ─── LANGUAGES & RUNTIMES ────────────────────────────────────────────────────
brew "mise"                   # Runtime version manager (node, ruby, python, etc.)
brew "go"

# ─── LANGUAGE TOOLS (shared by nvim + emacs LSPs) ─────────────────────────────
brew "stylua"                 # Lua formatter
brew "shellcheck"             # Shell linter
brew "shfmt"                  # Shell formatter

# ─── DEVOPS & INFRA ──────────────────────────────────────────────────────────
brew "direnv"                 # Per-directory env vars
brew "tmux"                   # Terminal multiplexer
brew "overmind"               # Process manager (Procfile)

# ─── ERLANG/ELIXIR BUILD DEPS ────────────────────────────────────────────────
brew "autoconf"
brew "wxwidgets"              # For Erlang observer
brew "openssl@3"
brew "libyaml"

# ─── FONTS ────────────────────────────────────────────────────────────────────
cask "font-fira-code"
cask "font-fira-code-nerd-font"
cask "font-hack-nerd-font"
cask "font-ubuntu"

# ─── MACOS APPS ──────────────────────────────────────────────────────────────
cask "aerospace"              # Tiling window manager
